Please go to the google share drive: https://drive.google.com/open?id=1qduJbz-WwrJU36vzp0s7Ac5omGwX3mMJ

The data file from assignment 1 is abalone_data.csv.

Another data is mushroom_data.csv.

The experiment is run in Python and using Scikit-Learn and Weka.

Use the python files to run the clustering analysis / Neural network.

Weka runs J48 with forward search: adding lower ranking components one by one.

The dataset are attached.

Use Python corresponding code to do PCA, ICA, Randomized Projection (RP) and Random Forest Filter (RF). Use the code to get transformed the data.